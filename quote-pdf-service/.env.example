PORT=8787
# Must match js/config.js → QUOTE_PDF_WEBHOOK_SECRET (copy the exact string from there).
WEBHOOK_SECRET=

# https://resend.com/api-keys
RESEND_API_KEY=

# Where the PDF notification is delivered — same inbox on Railway:
TO_EMAIL=1secfreelance@gmail.com

# Must be a domain you verify in Resend; for testing use:
# FROM_EMAIL=SEC Quotations <onboarding@resend.dev>
FROM_EMAIL=SEC Quotations <onboarding@resend.dev>
